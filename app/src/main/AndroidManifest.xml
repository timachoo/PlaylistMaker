<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name="com.example.playlistmaker.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlaylistMaker"
        tools:targetApi="33">
        <activity
            android:name=".presentstion.library.LibraryActivity"
            android:exported="false" />
        <activity
            android:name=".presentstion.search.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".presentstion.settings.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".presentstion.player.PlayerActivity"
            android:exported="false" />
        <activity
            android:name=".presentstion.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>